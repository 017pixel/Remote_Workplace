# Optional: bettet einen lokalen code-server als Editor in die Workbench ein.
# Nur nötig, wenn du den integrierten Editor nutzen möchtest.
# code-server muss installiert sein (https://github.com/coder/code-server).
[Unit]
Description=code-server (Workbench editor)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
Environment=HOME=__HOME__
ExecStart=__CODE_SERVER_BIN__ --config __REPO_ROOT__/config/code-server.yaml
Restart=on-failure
RestartSec=1s
TimeoutStopSec=20s
KillSignal=SIGTERM
UMask=0077

NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=full
ProtectKernelTunables=true
ProtectControlGroups=true
RestrictSUIDSGID=true
LockPersonality=true
RestrictAddressFamilies=AF_UNIX AF_INET AF_INET6 AF_NETLINK

[Install]
WantedBy=default.target
